version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: controle_produtos
    restart: always

    environment:
      POSTGRES_USER: radmin
      POSTGRES_PASSWORD: radmin
      POSTGRES_DB: produtos

    ports:
      - "5432:5432"
